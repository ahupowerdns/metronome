language: cpp
compiler:
  - gcc
  - clang
before_script:
 - sudo apt-get install --no-install-recommends libboost-all-dev
script:
 - autoreconf -i
 - automake -ac
 - ./configure
 - make
 - make check
